Para poder ejecutar los ejercicios, deben pocisionarce en la carpeta del mismo con la 
consola y ejecutar los comandos: 

`npm install`
`npm run dev`

Para la app de Rick And Morty correspondiente al ejemplo desarrollado en las playlist
del canal de YT de Egg deben ejecutar los siguientes comandos:

`npm install`
`npm start`